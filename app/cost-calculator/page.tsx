import CostCalculator from '@/components/CostCalculator';

export const metadata = {
  title: 'AI Cost Calculator | AI Performance Comparisons',
  description: 'Calculate and compare costs across different AI models. Estimate monthly expenses, compare API pricing vs self-hosting, and optimize your AI spending.',
};

export default function CostCalculatorPage() {
  return <CostCalculator />;
}
